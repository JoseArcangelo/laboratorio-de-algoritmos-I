base = float(input("Informe a base do retangulo:"))

altura = float(input("Informe a altura do retangulo:"))

area = base * altura

perimetro = (base * 2) + (altura * 2)

print("A area desse retangulo é", area, "e seu perimetro é", perimetro)
